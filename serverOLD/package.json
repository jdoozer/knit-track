{
  "name": "knit-track-express",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm run start",
    "server": "nodemon --inspect server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "test": "cd client && npm run test"
  },
  "dependencies": {
    "express": "^4.17.3",
    "uuid": "^3.3.3"
  },
  "devDependencies": {
    "concurrently": "^4.1.2",
    "morgan": "^1.9.1"
  }
}
